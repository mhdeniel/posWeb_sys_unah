
  # pos_sys_unah

  This is a code bundle for pos_sys_unah. The original project is available at https://www.figma.com/design/nECvGk87wcuvWLf3svLcxP/pos_sys_unah.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  